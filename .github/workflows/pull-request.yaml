name: Pull Request

on:
  pull_request:
    branches:
      - main

jobs:
  lint:
    name: Lint
    uses: ./.github/workflows/lint.yaml
    secrets: inherit

  test:
    name: Test
    uses: ./.github/workflows/test.yaml
    secrets: inherit
